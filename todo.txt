[] Optimize the filters: they all loop through all the layer data and apply something to the r g b
                         components. This could be abstracted. IntelliJ itself points out that there
                         is duplicated code.

[J] Fix all controller issues
[J] The unfilterLayer is somehow being modified by the filters. Figure out why this is.

[] Find out why smolLow2.ppm isn't getting deleted

[] Make it so the model itself doesn't do anything with files


[X] Figure out if it's necessary to have a HSLPixel class; I think giving the RGBAPixel class
   the ability to return its hue, saturation, and light values based on its RGB is better.